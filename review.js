// Since we only changed the internal logic of the `void (async () => {` search function within `pdf-viewer.tsx`
// and the tests in `apps/web` passed, we just need to confirm if there are any visual changes to verify.
// The visual layout is not changed, only the text search data extraction logic is changed.
// So step 2 of pre commit instructions (Verify Frontend) is not applicable or needed here.
