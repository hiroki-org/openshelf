codecov:
  require_ci_to_pass: true

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 100%
    patch:
      default:
        target: 0%

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: false

flags:
  api:
    paths:
      - apps/api/src/
    carryforward: true
  web:
    paths:
      - apps/web/src/
    carryforward: true
